/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.pexels.com',"www.patanjaliayurved.net"],
    },
}

module.exports = nextConfig
